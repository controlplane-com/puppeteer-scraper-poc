{
  "name": "puppeteer-scraper-poc-webapp",
  "version": "1.0.0",
  "description": "Web app that accepts a URL and triggers a Control Plane cron workload to scrape it with Puppeteer.",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^8.0.0",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0"
  }
}
