{
  "name": "puppeteer-scraper-poc-processor",
  "version": "1.0.0",
  "description": "Internal workload that receives raw HTML from the scraper, extracts title + links, stores in memory, and exposes GET /jobs/:id.",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.2",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0"
  }
}
